# MODELO DE DATOS — Fullpetro

> **Actualizado:** 27/08/2026
> Este documento describe el estado actual de la base de datos (PostgreSQL)
> y lo que está **planificado** para la fase backend de Combustible.
> Fuente de verdad de los DDL: `db/schema.sql`, `db/seed.sql`, `db/migrations/002_fuel.sql`,
> `db/migrations/003_fuel_english.sql`.
>
> **Decisión 27/08/2026 (aplicada):** los identificadores de BD del dominio Combustible
> pasan a **inglés** (mismo criterio que el código del frontend). La migración `003`
> renombra columnas y crea `fuel_pesada`; es **coordinada** con la fase backend
> (`queries.yaml` + BOs) y el frontend (`fuelService.js` + páginas fuel): **no aplicar
> por separado**.
> En flota pesada **no hay odómetro** (algunas unidades se miden por horas): la medida es
> un **único par valor+tipo** (`measurement_value` / `measurement_type`), no columnas
> separadas `kilometros` / `horas`.

---

## 0. Cómo se construye la BD hoy (orden de aplicación)

La BD **no se crea sola**: el backend solo sincroniza permisos (`permission.csv`)
al arrancar. Las tablas se aplican manualmente con `psql` en este orden:

```bash
psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/schema.sql          # 1. Seguridad/perfiles
psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/seed.sql            # 2. Datos iniciales (admin)
psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/migrations/002_fuel.sql  # 3. Combustible
psql -h $DB_HOST -U $DB_USER -d $DB_NAME -f db/migrations/003_fuel_english.sql  # 4. Modelo en inglés + fuel_pesada (COORDINADA con backend/front, ver §0 nota)
```

> ⚠️ **003_fuel_english.sql** renombra columnas de `vehicle`, `fuel_carga` y `fuel_foto`.
> Debe aplicarse **junto** con la actualización de `backend/config/queries.yaml`, las clases
> BO (`vehiculo.js`→`Vehicle`, `carga.js`→`Refuel`) y el frontend (`fuelService.js` + páginas
> fuel). Aplicarla con la BD vieja y el backend viejo rompe tx 81-90.

- Credenciales: variables del `.env` del backend (`DB_HOST`, `DB_PORT`, `DB_USER`, `DB_PASSWORD`, `DB_NAME`).
- La migración `001` **no existe** (la numeración arranca en 002; `schema.sql` es la base).
- Esquema usado por completo: **`public`**.
- Convención aplicada en todo el proyecto:
  - `id BIGINT GENERATED BY DEFAULT AS IDENTITY` siempre.
  - Soft-delete: columna `deleted_at TIMESTAMPTZ` (no se borra físicamente salvo decisiones puntuales).
  - Auditoría: `created_at` / `updated_at` con `DEFAULT NOW()`.
  - `created_by BIGINT REFERENCES "user"(id)` en tablas de negocio que registran quién hizo la acción.
  - Checks e índices sobre columnas de búsqueda.

---

## 1. Resumen general

### 1.1 Conteo de tablas

| Dominio | Archivo | Tablas |
|---|---|---|
| Seguridad / Perfiles / Permisos | `db/schema.sql` | 15 |
| Combustible (flota liviana) | `db/migrations/002_fuel.sql` | 3 |
| **Total actual** | | **18** |
| Combustible (fase backend) | `db/migrations/003_fuel_english.sql` | +1 tabla (`fuel_pesada`) + renombrado de columnas a inglés + `fleet_type`/`responsible_id`/`fuel_type`/`department` |
| Combustible (tanque de gasoil) | fase 2 (pendiente) | +2 tablas |

### 1.2 Tablas actuales (18)

**Seguridad (15):**
1. `person`
2. `user`
3. `profile`
4. `user_profile`
5. `subsystem`
6. `class`
7. `method`
8. `subsystem_class`
9. `class_method`
10. `method_profile`
11. `transaction`
12. `option`
13. `option_profile`
14. `menu`
15. `option_menu`

**Combustible (3):**
16. `vehicle`
17. `fuel_carga`
18. `fuel_foto`

---

## 2. Dominios de validación (schema.sql)

Definidos a nivel de BD y **usados por las tablas de seguridad**:

| Dominio | Tipo | Regla |
|---|---|---|
| `email_address` | VARCHAR(150) | `^[A-Z0-9._%+\-]+@[A-Z0-9.\-]+\.[A-Z]{2,}$` |
| `document_identifier` | VARCHAR(30) | `^[A-Za-z0-9\-]{5,30}$` (mín. 5 caracteres) |
| `phone_number` | VARCHAR(30) | `^\+?[0-9 ()\-]{7,20}$` |

> ⚠️ Nota: `document_identifier` exige **mínimo 5 caracteres**. Como la cédula ya no se captura (la autogenera el front como `PER-XXXX`), el patrón cumple.
> Dominios y tablas de **negocio** (`vehicle`, `fuel_*`) están en esquema `public` y no usan los dominios; usan `VARCHAR`/`NUMERIC` directos.

---

## 3. Tablas de Seguridad / Perfiles (`schema.sql`)

### 3.1 `person` — Personas (registro general)
Uso actual: responsable de combustible (flota liviana). Futuro: cualquier otra sección.

| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGINT PK | identidad |
| `document_id` | document_identifier | **NOT NULL, UNIQUE** — cédula (hoy autogenerada por el front `PER-*`) |
| `first_name` | VARCHAR(100) | NOT NULL |
| `last_name` | VARCHAR(100) | NOT NULL |
| `phone` | phone_number | NULL |
| `degree` | VARCHAR(150) | cargo (colonias: Cargo en el front) |
| `address` | TEXT | NULL |
| `birth_date` | DATE | NULL |
| `created_at` / `updated_at` | TIMESTAMPTZ | DEFAULT NOW() |
| `deleted_at` | TIMESTAMPTZ | soft-delete |

Restricciones: `uq_person_document_id`, `ck_person_first_name_not_blank`, `ck_person_last_name_not_blank`.

**Planificado (fase backend):**
- ➕ `department VARCHAR(150)` — departamento (catálogo precargado actualmente en el front: `src/lib/catalogs.js`). La columna queda **creada por `003_fuel_english.sql`**.
- El CRUD completo usa tx 91-94 (`Security/Persona/*`).

### 3.2 `user` — Usuarios del sistema
Sesión con correo (empresarial) y contraseña; acceso limitado por rol.

| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGINT PK | identidad |
| `name` | VARCHAR(60) | NOT NULL — **identificador de login hoy** (username) |
| `email` | email_address | NULL — correo empresarial |
| `password_hash` | TEXT | NOT NULL — bcrypt |
| `is_solvency` | BOOLEAN | DEFAULT TRUE |
| `is_active` | BOOLEAN | DEFAULT TRUE |
| `person_id` | BIGINT FK → `person(id)` | ON DELETE SET NULL |
| `created_at` / `updated_at` | TIMESTAMPTZ | DEFAULT NOW() |
| `deleted_at` | TIMESTAMPTZ | soft-delete |

Restricciones: `ck_user_name_not_blank`, `ck_user_password_hash_not_blank`.

**Planificado (fase backend):**
- ➕ `first_name` / `last_name` (hoy el front guarda `name` = nombre+apellido concatenados y autogenera el username).
- Decidir: migrar el **login por `email`** (el flujo actual autentica por `name`).
- El CRUD usa tx 31-36 (`Users/Usuario/*`).

### 3.3 `profile` — Roles
| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGINT PK | |
| `name` | VARCHAR(100) | NOT NULL, UNIQUE (ej. `admin`) |
| `description` | TEXT | NULL |
| `is_active` | BOOLEAN | DEFAULT TRUE |

### 3.4 `user_profile` — Asignación usuario ↔ rol (N:M)
| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGINT PK | |
| `user_id` | BIGINT FK → `user(id)` | ON DELETE RESTRICT |
| `profile_id` | BIGINT FK → `profile(id)` | ON DELETE RESTRICT |
| | | `uq (user_id, profile_id)` |

### 3.5 Matriz de permisos (subsistema → clase → método → perfil)

El motor de permisos modela la arquitectura de `permission.csv`:

- `subsystem` — módulos (ej. `Security`, `Fuel`, `Users`…): `id`, `name` (UNIQUE), `description`.
- `class` — clases de negocio (ej. `Person`, `Carga`…): `id`, `name` (UNIQUE), `description`.
- `method` — métodos (ej. `createCarga`…): `id`, `name` (UNIQUE), `description`.
- `subsystem_class` — N:M subsistema↔clase: `id`, `subsystem_id` (FK), `class_id` (FK), `uq(subsystem_id, class_id)`.
- `class_method` — N:M clase↔método: `id`, `class_id` (FK), `method_id` (FK), `uq(class_id, method_id)`.
- `method_profile` — N:M método↔perfil (es la matriz de permisos efectiva): `id`, `method_id` (FK), `profile_id` (FK), `uq(method_id, profile_id)`.
- `transaction` — registro de la transacción (alcance subsistema-clase-método):
  `id`, `subsystem_id` (FK), `class_id` (FK), `method_id` (FK), `created_at`, `updated_at`, `uq(subsystem_id, class_id, method_id)`.
  Es la tabla que el backend sincroniza desde `permission.csv` al arrancar (function `syncPermissions`): la **columna `id` del CSV = ID de la transacción** que usa el front (`transaction_id`).

### 3.6 Menús y opciones por perfil (navegación)

- `option` — opción de navegación: `id`, `name` (UNIQUE), `description`.
- `option_profile` — N:M opción↔perfil: `id`, `profile_id` (FK), `option_id` (FK), `uq(profile_id, option_id)`.
- `menu` — menú: `id`, `name`, `description`, `subsystem_id` (FK → `subsystem`), `uq(name, subsystem_id)`.
- `option_menu` — N:M opción↔menú: `id`, `option_id` (FK), `menu_id` (FK), `uq(option_id, menu_id)`.

> Estas tablas de navegación están definidas pero **aún no son pobladas** por el frontend (el sidebar es estático hoy).

---

## 4. Tablas de Combustible — Flota Liviana (`002_fuel.sql` + `003_fuel_english.sql`)

> **Estado del esquema tras aplicar la migración 003 (modelo en inglés).** Los nombres de
> columnas se muestran en inglés (renombrados por 003). No aplicar 003 por separado del
> backend/frontend (ver §0).

### 4.1 `vehicle` — Unidades (vehículos livianos y próximamente pesados)
| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGSERIAL PK | |
| `code` | VARCHAR(20) | NOT NULL, UNIQUE (ej. `VH-001`) — antes `codigo` |
| `name` | VARCHAR(100) | NOT NULL — antes `nombre` |
| `plate` | VARCHAR(20) | NULL — antes `placa` |
| `tank_capacity_liters` | NUMERIC(8,2) | NOT NULL DEFAULT 0 — antes `tanque_capacidad_litros` |
| `fleet_type` | VARCHAR(20) | NOT NULL DEFAULT `liviana` — ➕ por 003; valores `liviana \| pesada` (hoy solo lo maneja el front en localStorage) |
| `is_active` | BOOLEAN | DEFAULT TRUE |
| `created_at` / `updated_at` | TIMESTAMPTZ | DEFAULT NOW() |
| `deleted_at` | TIMESTAMPTZ | soft-delete |

### 4.2 `fuel_carga` — Llenados de combustible (flota liviana)
| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGSERIAL PK | |
| `vehicle_id` | BIGINT FK → `vehicle(id)` | NOT NULL |
| `filled_at` | TIMESTAMPTZ | NOT NULL DEFAULT NOW() — antes `fecha`; guarda fecha completa (fecha+hora capturadas por el usuario); sustituye al plan de `hora_llenado` separada |
| `liters` | NUMERIC(8,2) | NOT NULL, `> 0` — antes `litros` |
| `tank_full` | BOOLEAN | DEFAULT FALSE — antes `tanque_lleno` |
| `station` | VARCHAR(100) | NULL — antes `estacion` |
| `odometer` | NUMERIC(10,1) | NULL — antes `odometro` |
| `amount` | NUMERIC(12,2) | NULL — antes `monto` |
| `notes` | TEXT | NULL — antes `observaciones` |
| `responsible_id` | BIGINT FK → `person(id)` | ➕ por 003 (fase backend); responsable (seguridad → personas) |
| `fuel_type` | VARCHAR(20) | NOT NULL DEFAULT `gasolina` — ➕ por 003; gasolina/diesel |
| `created_by` | BIGINT FK → `user(id)` | NULL |
| `created_at` | TIMESTAMPTZ | DEFAULT NOW() — **hora de REGISTRO (solo visible en BD)** |
| `deleted_at` | TIMESTAMPTZ | soft-delete |

Índice: `idx_fuel_carga_vehicle (vehicle_id, filled_at DESC)`.

### 4.3 `fuel_foto` — Fotos de los tickets de llenado
| Campo | Tipo | Notas |
|---|---|---|
| `id` | BIGSERIAL PK | |
| `refuel_id` | BIGINT FK → `fuel_carga(id)` | NOT NULL — antes `carga_id` |
| `url` | VARCHAR(500) | NOT NULL |
| `thumbnail_url` | VARCHAR(500) | NULL |
| `mime_type` | VARCHAR(30) | DEFAULT `image/jpeg` |
| `size_bytes` | BIGINT | NULL |
| `uploaded_by` | BIGINT FK → `user(id)` | NULL |
| `uploaded_at` | TIMESTAMPTZ | DEFAULT NOW() |

Índice: `idx_fuel_foto_refuel (refuel_id)`.

---

## 5. Tablas de la fase backend

### 5.1 `fuel_pesada` — Llenados de flota pesada (pesada)

> **Creada por `003_fuel_english.sql`** (aplicada de forma coordinada con backend + frontend).
> **Medida única valor+tipo** (no hay odómetro en pesada; hay unidades que se rigen por horas).

```sql
CREATE TABLE public.fuel_pesada (
  id BIGSERIAL PRIMARY KEY,
  transaction_no VARCHAR(40),          -- nomenclatura / id de transacción (la entrega el usuario)
  vehicle_id BIGINT NOT NULL REFERENCES public.vehicle(id),
  filled_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  requester VARCHAR(120),              -- solicitante: distinto al responsable de liviana
  fuel_type VARCHAR(20) NOT NULL DEFAULT 'gasoil',
  measurement_value NUMERIC(10,1),     -- valor de la medida (km u horas)
  measurement_type VARCHAR(10) NOT NULL DEFAULT 'km',  -- 'km' | 'horas'
  gallons NUMERIC(10,2) NOT NULL,
  notes TEXT,
  created_by BIGINT REFERENCES public."user"(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ,
  CONSTRAINT ck_fuel_pesada_gallons_positive CHECK (gallons > 0),
  CONSTRAINT ck_fuel_pesada_measurement_type CHECK (measurement_type IN ('km', 'horas'))
);
```

Índice: `idx_fuel_pesada_vehicle (vehicle_id, filled_at DESC)`.

- Fotos opcionales: decidir si se generaliza `fuel_foto` (columna `pesada_id`) o se crea `fuel_pesada_foto`.
- Conversión: `liters = gallons × 3.78541` (solo visualización).

### 5.2 `fuel_tanque` y `fuel_tanque_movimiento` — Tanque de gasoil (fase 2, pendiente)

> SQL de referencia, todavía **no aplicado** (fase 2). Nombres en inglés (mismo criterio).

```sql
CREATE TABLE public.fuel_tanque (
  id BIGSERIAL PRIMARY KEY,
  code VARCHAR(20) UNIQUE NOT NULL,
  name VARCHAR(100) NOT NULL,
  capacity_liters NUMERIC(10,2) NOT NULL,
  min_level_alert_liters NUMERIC(10,2) NOT NULL DEFAULT 0,
  is_active BOOLEAN NOT NULL DEFAULT TRUE,
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
  deleted_at TIMESTAMPTZ
);

CREATE TABLE public.fuel_tanque_movimiento (
  id BIGSERIAL PRIMARY KEY,
  tank_id BIGINT NOT NULL REFERENCES public.fuel_tanque(id),
  type VARCHAR(10) NOT NULL,           -- 'in' (abastecimiento) | 'out' (despacho); UI muestra Entrada/Salida
  amount_liters NUMERIC(10,2) NOT NULL,
  referent_type VARCHAR(20),           -- 'pesada' | 'manual' | ...
  referent_id BIGINT,                  -- fuel_pesada.id si el despacho fue un llenado pesado
  note TEXT,
  created_by BIGINT REFERENCES public."user"(id),
  created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()
);
```

- Nivel = Σ entradas − Σ salidas (se **calcula** al consultar, no se almacena).
- Alerta cuando `nivel ≤ nivel_min_alerta_litros`.

---

## 6. Diagrama de relaciones (resumen)

```
person ──────< user ──────< user_profile >────── profile
                          (usuario N:M rol)

profile >──── method_profile >──── method >───── class >──── subsystem
        (permisos: método disponible por perfil)

subsystem >──── menu >──── option_menu >──── option
profile >──── option_profile >──── option      (navegación por perfil)

vehicle ──────< fuel_carga ──────< fuel_foto
        1            N               N
vehicle ──────< fuel_pesada ───────(fotos opcionales)
vehicle ──────< (fleet_type: liviana | pesada)
person ──────< fuel_carga.responsible_id     (fase backend)
user  ──────created_by──> fuel_carga / fuel_pesada (auditoría)
```

### 6.1 Notas de cardinalidad

| Relación | Tipo | Regla |
|---|---|---|
| `person` 1—N `user` | 1:N | `person_id` ON DELETE SET NULL |
| `user` N—M `profile` | N:M | vía `user_profile` (RESTRICT) |
| `profile` N—M `method` | N:M | vía `method_profile` (matriz de permisos) |
| `vehicle` 1—N `fuel_carga` | 1:N | FK `vehicle_id` |
| `fuel_carga` 1—N `fuel_foto` | 1:N | FK `refuel_id` |
| `user` 1—N `fuel_carga.created_by` | 1:N | auditoría (NULL permitido) |
| `person` 1—N `fuel_carga.responsible_id` | 1:N | **fase backend** |

---

## 7. Datos iniciales (seed.sql)

| Dato | Valor |
|---|---|
| Persona | `ADMIN-00001` — Administrador Sistema |
| Usuario | `admin01` / `admin01@fullpetro.com` / **Admin1234** (hash bcrypt) |
| Perfil | `admin` — Administrador del sistema |
| Vinculación | `user_profile` admin01 ↔ admin |

> El perfil `admin` se **re-sincroniza** con `backend/config/permission.csv` al arrancar el backend (cada fila del CSV crea/actualiza la cadena subsystem→class→method→method_profile→transaction). Los `id` 1-105 del CSV son los `transaction_id` que usa el frontend.

---

## 8. Transacciones vigentes (permission.csv ↔ frontend)

| Rango | Subsistema | Uso |
|---|---|---|
| 1-4 | Security (Person, Profile) | persona crear, crear perfil, asignar perfil, obtener perfil |
| 5-22, 23-30, 37-80 | Otros módulos (Inventario, Préstamos, etc.) | legado, sin BO implementado |
| 31-36 | Users/Usuario | CRUD usuarios (frontend listo, **BO pendiente**) |
| 81-90 | Fuel (Vehiculo 81-84/90, Carga 85-89) | **funcional end-to-end** |
| 91-95 | Security/Persona + Fuel/Carga/getCargaById | personas listar/obtener/actualizar/eliminar; detalle de carga (**BO pendiente**) |
| 96-100 | Fuel/Pesada | CRUD pesada (**BO pendiente**) |
| 101-105 | Security/Profile | listar/actualizar/eliminar rol; quitar rol; roles por usuario (**BO pendiente**) |